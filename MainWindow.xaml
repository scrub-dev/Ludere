<Window x:Class="NowPlaying.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" 
        Height="600" Width="920"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="CanMinimize"
        xmlns:vm="clr-namespace:NowPlaying.ViewModels">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Border Background="#FF202030" CornerRadius="15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" CornerRadius="15,15,0,0" Style="{StaticResource BackgroundAccent}" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Column="2" FlowDirection="RightToLeft">
                        <StackPanel.Resources>
                            <Style TargetType="{x:Type Button}">
                                <Setter Property="Margin" Value="5"/>
                            </Style>
                        </StackPanel.Resources>
                        <Button Width="30" BorderBrush="Transparent" Command="{Binding CloseApplicationCommand}" Style="{StaticResource ButtonClose}">
                            <Button.Resources>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="10"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                </Style>
                            </Button.Resources>
                            <Button.Content>
                                <Image Width="20" Height="20" Source="/Images/cross.png"/>
                            </Button.Content>
                        </Button>
                        <Button Width="30" BorderBrush="Transparent" Click="BtnMinimise_Click" Style="{StaticResource ButtonMinimise}">
                            <Button.Resources>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Button.Resources>
                            <Button.Content>
                                <Image Width="20" Height="20" Source="/Images/minimise.png"/>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <Border CornerRadius="0,0,15,15" Style="{StaticResource BackgroundAccent}" Grid.Row="2" Grid.ColumnSpan="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="4*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Panel.ZIndex="1000">
                        <Button Margin="10" Width="100" Content="Help" Command="{Binding HelpButtonCommand}" Style="{StaticResource ButtonHelp}"/>
                    </Border>
                    <StackPanel Orientation="Horizontal" Grid.Column="2" FlowDirection="RightToLeft" >
                        <Button Width="100" Margin="10,10,10,10" Style="{StaticResource ButtonStart}">Start</Button>
                        <Button Width="100" Margin="0,10,0,10" Style="{StaticResource ButtonStop}">Stop</Button>
                    </StackPanel>
                    <Border Style="{StaticResource BackgroundAccent}" Grid.Column="1">
                        <Border Style="{StaticResource BackgroundMain}" Margin="0,10,0,10" CornerRadius="10">
                            <Label VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" Foreground="White" Padding="5,0,0,0" FontFamily="Cascadia Code" FontWeight="Light" Content="{Binding CurrentState}"/>
                        </Border>
                    </Border>
                </Grid>
            </Border>
            <Border Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" Style="{StaticResource BackgroundAccent}" CornerRadius="0,0,0,15" Panel.ZIndex="-1000">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="9*"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="0" 
                               HorizontalAlignment="Center" 
                               Foreground="White" 
                               FontFamily="/Fonts/#Poppins" 
                               FontWeight="Light" 
                               FontSize="36" 
                               Margin="10" 
                               Text=" Ludere " 
                               VerticalAlignment="Top">
                        <TextBlock.TextDecorations>
                            <TextDecoration Location="Underline" PenOffset="3">
                                <TextDecoration.Pen>
                                    <Pen Brush="#FF202030" Thickness="3"/>
                                </TextDecoration.Pen>
                            </TextDecoration>
                        </TextBlock.TextDecorations>
                    </TextBlock>
                    <StackPanel Grid.Row="0" Margin="0,-90,0,0" VerticalAlignment="Center">
                        <!--<Button Style="{StaticResource ButtonMain}" Height="40" Margin="5" Content="Home" x:Name="BtnNavigate_Home" Click="BtnNav_Click"/>-->
                        <!--<Button Style="{StaticResource ButtonMain}" Height="40" Margin="5" Content="Spotify Settings" x:Name="BtnNavigate_SpotifySettings" Click="BtnNav_Click"/>-->
                        <!--<Button Style="{StaticResource ButtonMain}" Height="40" Margin="5" Content="Server Settings" x:Name="BtnNavigate_ServerSettings" Click="BtnNav_Click"/>-->
                        <!--<Button Style="{StaticResource ButtonMain}" Height="40" Margin="5" Content="Style Settings" x:Name="BtnNavigate_StyleSettings" Click="BtnNav_Click"/>-->
                        <!--<Button Style="{StaticResource ButtonMain}" Height="40" Margin="5" Content="Custom Styles" x:Name="BtnNavigate_CustomStyle" Click="BtnNav_Click"/>-->
                        <!--<Button Style="{StaticResource ButtonMain}"  Margin="10" Height="40" Content="Help" Click="BtnHelp_Click"/>-->
                        <RadioButton Style="{StaticResource MenuButton}" Content="Home" IsChecked="True"/>
                        <RadioButton Style="{StaticResource MenuButton}" Content="Spotify Settings"/>
                        <RadioButton Style="{StaticResource MenuButton}" Content="Server Settings"/>
                        <RadioButton Style="{StaticResource MenuButton}" Content="Style Settings"/>
                        <RadioButton Style="{StaticResource MenuButton}" Content="Custom Styles"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Column="1" Grid.Row="1" CornerRadius="15" Style="{StaticResource BackgroundMain}">
                <DockPanel>
                    <Frame x:Name="FrameMain" NavigationUIVisibility="Hidden" Panel.ZIndex="1"/>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
